# Birthday Tribute

A cinematic birthday tribute for Nitin.

## Commands

```bash
npm run dev
npm run build
npm run test
npm run remotion:studio
npm run render:landscape
npm run render:story
```

Run `npm run score` to regenerate the local cinematic score at `public/audio/nitin-cinematic-score.wav`.
